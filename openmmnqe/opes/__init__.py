"""
Vendored OPES post-processing scripts (see the script headers for provenance).

These are standalone command-line tools, not importable APIs;
``openmmnqe.plumed`` invokes ``FES_from_State.py`` as a subprocess. Making
this directory a package ensures the scripts ship in wheels.
"""
